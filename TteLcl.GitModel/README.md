# TteLcl.GitModel

This library provides an model for items and concepts in GIT repositories.
This library is designed to coexist alongside libgit2sharp, avoiding
name conflicts, but does not depend on it.
